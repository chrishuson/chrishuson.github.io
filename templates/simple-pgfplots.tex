\documentclass{article}
\usepackage{pgfplots}\pgfplotsset{compat=newest}
\begin{document}
%\newcommand{\mya}{2}

I have a nice parabola:
\[ y =  x^2 + 3 x + 1 \]
which is drawn like:

\begin{tikzpicture}
    \begin{axis}[width=9cm,
        xmin=-3, xmax=3,
        ymin=-10, ymax=11,
        domain=-5:10,
        samples=100,
        axis x line = center,
        axis y line = center,
        xlabel = {$x$},
        ylabel = {$y$},
        ]
        \addplot[blue, thick] { x*x + 2*x -3 };
    \end{axis}
\end{tikzpicture}
\end{document}